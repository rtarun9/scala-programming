object list_obj {
    def main(args: Array[String]) = {
        var elem: Int = scala.io.StdIn.readInt()
        printf("%d", elem);

        var numbers: List[Int] = List[Int]();
        for (i <- 0 to elem) {
            
        }
    }
}